import { ReactElement } from 'react';
import { CalYearContainer } from 'components/cal-main/cal-year/styles';

export default function CalYear(): ReactElement {
    return (
        <CalYearContainer data-testid="cal-main">
            <>cal year</>
        </CalYearContainer>
    );
}
