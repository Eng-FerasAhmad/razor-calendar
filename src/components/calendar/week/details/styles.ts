import { styled } from '@mui/material/styles';

export const WeekAppointmentDetailsContainer = styled('div')({
    position: 'relative',
    width: '100%',
    minWidth: '400px',
    height: '100%',
});
