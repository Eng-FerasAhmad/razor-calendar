import { styled } from 'styled-components';
import { color } from 'style/color';

export const CalTypeListContainer = styled.div`
    width: fit-content;
    height: 30px;
    border: 1px solid ${color.borderLight};
    border-radius: 5px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    padding: 2px 10px;
`;
