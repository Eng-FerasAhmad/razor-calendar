import { ReactElement } from 'react';
import { LogoContainer } from 'components/cal-header/logo/styles';

export default function Logo(): ReactElement {
    return (
        <LogoContainer>
            <>Logo</>
        </LogoContainer>
    );
}
