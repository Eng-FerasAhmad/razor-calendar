import { ReactElement } from 'react';
import { CalNavigatorContainer } from 'components/cal-header/cal-navigator/styles';

export default function CalNavigator(): ReactElement {
    return (
        <CalNavigatorContainer>
            <>CalNavigator</>
        </CalNavigatorContainer>
    );
}
