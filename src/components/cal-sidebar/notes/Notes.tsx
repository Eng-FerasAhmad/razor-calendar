import { ReactElement } from 'react';
import { NotesContainer } from 'components/cal-sidebar/notes/styles';

export default function Notes(): ReactElement {
    return (
        <NotesContainer>
            <></>
        </NotesContainer>
    );
}
