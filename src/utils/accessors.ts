export const getEventTitle = (event: { title: string }) => event.title;
export const getEventStart = (event: { start: Date }) => event.start;
export const getEventEnd = (event: { end: Date }) => event.end;
